# Frame-in-order X Bot — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** A partir de um vídeo + `.srt`, extrair 1 frame/segundo em ordem cronológica e postar um por vez no X num intervalo configurável, cada post com a imagem + a legenda ativa naquele instante.

**Architecture:** Duas etapas desacopladas. `prepare.py` roda uma vez: ffmpeg extrai os frames e um `manifest.json` casa cada frame com sua legenda. `post.py` roda a cada 1h30 (via agendador externo): lê o manifest + `state.json`, posta o próximo frame no X e avança o índice só em caso de sucesso.

**Tech Stack:** Python 3.13 (chamado via `py` no Windows), ffmpeg (externo), libs `tweepy` e `srt`, `pytest` para testes.

## Global Constraints

- Python 3.13, invocado como `py` no Windows (não `python`).
- Apenas duas dependências Python de runtime: `tweepy`, `srt`. Nada além.
- `ffmpeg` é dependência externa (do sistema), não pip.
- Frames numerados zero-padded a 5 dígitos: `frames/00001.jpg` = segundo 0.
- Timestamp do frame N (1-indexed) = segundo `N-1` do vídeo.
- Legenda ativa = primeira cue com `start ≤ seconds ≤ end`; sem cue → `text = ""`.
- Segredos só em `.env`, nunca commitado.
- `post.py`: em falha de postagem, **não** avança o estado.

---

### Task 1: Estrutura do projeto e dependências

**Files:**
- Create: `requirements.txt`
- Create: `.gitignore`
- Create: `.env.example`
- Create: `README.md`

- [ ] **Step 1: Criar `requirements.txt`**

```
tweepy==4.14.0
srt==3.5.3
pytest==8.3.4
```

- [ ] **Step 2: Criar `.gitignore`**

```
.env
frames/
manifest.json
state.json
__pycache__/
*.pyc
.venv/
```

- [ ] **Step 3: Criar `.env.example`**

```
# Chaves de desenvolvedor do X (https://developer.x.com) — copie para .env e preencha
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_TOKEN_SECRET=
```

- [ ] **Step 4: Criar `README.md`**

```markdown
# Bot every frame in order

Extrai 1 frame/segundo de um vídeo e posta no X em ordem, com a legenda.

## Setup

1. Instale o ffmpeg e garanta que `ffmpeg` está no PATH.
2. `py -m venv .venv && .venv\Scripts\activate`
3. `py -m pip install -r requirements.txt`
4. Copie `.env.example` para `.env` e preencha as 4 chaves do X.

## Uso

```
py prepare.py video.mp4 legenda.srt
py post.py
```

`prepare.py` roda uma vez. Um agendador (Task Scheduler / GitHub Actions / cron)
chama `py post.py` a cada 1h30.
```

- [ ] **Step 5: Criar o venv e instalar**

Run: `py -m venv .venv && .venv/Scripts/python.exe -m pip install -r requirements.txt`
Expected: instala tweepy, srt, pytest sem erro.

- [ ] **Step 6: Commit**

```bash
git add requirements.txt .gitignore .env.example README.md
git commit -m "chore: project scaffolding and deps"
```

---

### Task 2: Casamento legenda ↔ segundo (`subtitles.py`)

Módulo puro (sem I/O de rede nem ffmpeg), fácil de testar. Responsável só por: dado o texto de um `.srt`, devolver a legenda ativa num dado segundo.

**Files:**
- Create: `subtitles.py`
- Test: `test_subtitles.py`

**Interfaces:**
- Produces: `parse_srt(srt_text: str) -> list[srt.Subtitle]` e `text_at(cues: list[srt.Subtitle], seconds: int) -> str`.

- [ ] **Step 1: Escrever o teste que falha**

```python
# test_subtitles.py
from subtitles import parse_srt, text_at

SAMPLE = """1
00:00:00,000 --> 00:00:02,000
Olá mundo

2
00:00:05,000 --> 00:00:07,500
Segunda fala
"""

def test_text_at_within_cue():
    cues = parse_srt(SAMPLE)
    assert text_at(cues, 0) == "Olá mundo"
    assert text_at(cues, 1) == "Olá mundo"

def test_text_at_silence_returns_empty():
    cues = parse_srt(SAMPLE)
    assert text_at(cues, 3) == ""      # entre as duas falas
    assert text_at(cues, 100) == ""    # depois do fim

def test_text_at_boundary_inclusive():
    cues = parse_srt(SAMPLE)
    assert text_at(cues, 2) == "Olá mundo"   # 00:00:02,000 = fim inclusivo
    assert text_at(cues, 5) == "Segunda fala"

def test_multiline_cue_joined():
    srt_text = """1
00:00:00,000 --> 00:00:01,000
linha um
linha dois
"""
    cues = parse_srt(srt_text)
    assert text_at(cues, 0) == "linha um linha dois"
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `.venv/Scripts/python.exe -m pytest test_subtitles.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'subtitles'`.

- [ ] **Step 3: Implementar `subtitles.py`**

```python
import srt


def parse_srt(srt_text: str) -> list:
    """Faz o parse de um SRT em lista de srt.Subtitle, ordenada por início."""
    return list(srt.parse(srt_text))


def text_at(cues: list, seconds: int) -> str:
    """Texto da legenda ativa em `seconds` (start<=seconds<=end), ou "" se silêncio.

    Multi-linha é unido por espaço. Intervalos inclusivos nas duas pontas.
    """
    for cue in cues:
        if cue.start.total_seconds() <= seconds <= cue.end.total_seconds():
            return " ".join(cue.content.splitlines()).strip()
    return ""
```

- [ ] **Step 4: Rodar e ver passar**

Run: `.venv/Scripts/python.exe -m pytest test_subtitles.py -v`
Expected: 4 passed.

- [ ] **Step 5: Commit**

```bash
git add subtitles.py test_subtitles.py
git commit -m "feat: subtitle-to-second matching"
```

---

### Task 3: Extração de frames + manifest (`prepare.py`)

**Files:**
- Create: `prepare.py`
- Test: `test_prepare.py`

**Interfaces:**
- Consumes: `subtitles.parse_srt`, `subtitles.text_at` (da Task 2).
- Produces: `build_manifest(frame_files: list[str], srt_text: str) -> list[dict]` (itens `{"index","file","seconds","text"}`); `extract_frames(video_path, out_dir) -> list[str]`; CLI `py prepare.py <video> <srt>` que grava `manifest.json`.

- [ ] **Step 1: Escrever o teste que falha (lógica pura do manifest)**

```python
# test_prepare.py
from prepare import build_manifest

SAMPLE = """1
00:00:00,000 --> 00:00:01,000
oi

2
00:00:03,000 --> 00:00:04,000
tchau
"""

def test_build_manifest_matches_text_by_second():
    # cue 1: seg 0–1 ("oi"); cue 2: seg 3–4 ("tchau"); seg 2 = silêncio
    frames = ["frames/00001.jpg", "frames/00002.jpg",
              "frames/00003.jpg", "frames/00004.jpg", "frames/00005.jpg"]
    m = build_manifest(frames, SAMPLE)
    assert len(m) == 5
    assert m[0] == {"index": 0, "file": "frames/00001.jpg", "seconds": 0, "text": "oi"}
    assert m[1]["text"] == "oi"        # segundo 1 ainda dentro da cue 1
    assert m[2]["text"] == ""          # segundo 2 = silêncio
    assert m[3]["text"] == "tchau"     # segundo 3 = início da cue 2
    assert m[4]["text"] == "tchau"     # segundo 4 = fim inclusivo da cue 2
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `.venv/Scripts/python.exe -m pytest test_prepare.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'prepare'`.

- [ ] **Step 3: Implementar `prepare.py`**

```python
import json
import subprocess
import sys
from pathlib import Path

from subtitles import parse_srt, text_at

FRAMES_DIR = "frames"
MANIFEST = "manifest.json"


def extract_frames(video_path: str, out_dir: str = FRAMES_DIR) -> list[str]:
    """Extrai 1 frame/segundo via ffmpeg -> out_dir/NNNNN.jpg. Retorna os caminhos ordenados."""
    out = Path(out_dir)
    out.mkdir(exist_ok=True)
    cmd = [
        "ffmpeg", "-i", video_path,
        "-vf", "fps=1",
        "-q:v", "2",
        str(out / "%05d.jpg"),
    ]
    result = subprocess.run(cmd, capture_output=True, text=True)
    if result.returncode != 0:
        raise RuntimeError(f"ffmpeg falhou:\n{result.stderr}")
    return sorted(str(p) for p in out.glob("*.jpg"))


def build_manifest(frame_files: list[str], srt_text: str) -> list[dict]:
    """Casa cada frame (segundo = índice) com a legenda ativa. Frames em ordem."""
    cues = parse_srt(srt_text)
    manifest = []
    for i, file in enumerate(sorted(frame_files)):
        manifest.append({
            "index": i,
            "file": file.replace("\\", "/"),
            "seconds": i,
            "text": text_at(cues, i),
        })
    return manifest


def main():
    if len(sys.argv) != 3:
        print("uso: py prepare.py <video> <srt>")
        sys.exit(1)
    video, srt_path = sys.argv[1], sys.argv[2]
    srt_text = Path(srt_path).read_text(encoding="utf-8")
    frames = extract_frames(video)
    manifest = build_manifest(frames, srt_text)
    Path(MANIFEST).write_text(json.dumps(manifest, ensure_ascii=False, indent=2), encoding="utf-8")
    print(f"{len(manifest)} frames -> {MANIFEST}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: Rodar e ver passar**

Run: `.venv/Scripts/python.exe -m pytest test_prepare.py -v`
Expected: 1 passed.

- [ ] **Step 5: Teste manual do ffmpeg (opcional, precisa de vídeo real)**

Run: `.venv/Scripts/python.exe prepare.py sample.mp4 sample.srt`
Expected: cria `frames/` e `manifest.json`. Pular se não houver vídeo de teste à mão.

- [ ] **Step 6: Commit**

```bash
git add prepare.py test_prepare.py
git commit -m "feat: frame extraction and manifest builder"
```

---

### Task 4: Avanço de estado (`state.py`)

Lógica de estado isolada da API do X, pra poder testar o "avança em sucesso, mantém em falha" sem rede.

**Files:**
- Create: `state.py`
- Test: `test_state.py`

**Interfaces:**
- Produces: `load_next_index(path="state.json") -> int` (0 se não existe); `save_next_index(index: int, path="state.json") -> None`.

- [ ] **Step 1: Escrever o teste que falha**

```python
# test_state.py
from state import load_next_index, save_next_index

def test_default_is_zero(tmp_path):
    assert load_next_index(tmp_path / "state.json") == 0

def test_roundtrip(tmp_path):
    p = tmp_path / "state.json"
    save_next_index(7, p)
    assert load_next_index(p) == 7
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `.venv/Scripts/python.exe -m pytest test_state.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'state'`.

- [ ] **Step 3: Implementar `state.py`**

```python
import json
from pathlib import Path

STATE = "state.json"


def load_next_index(path=STATE) -> int:
    p = Path(path)
    if not p.exists():
        return 0
    return json.loads(p.read_text())["next_index"]


def save_next_index(index: int, path=STATE) -> None:
    Path(path).write_text(json.dumps({"next_index": index}))
```

- [ ] **Step 4: Rodar e ver passar**

Run: `.venv/Scripts/python.exe -m pytest test_state.py -v`
Expected: 2 passed.

- [ ] **Step 5: Commit**

```bash
git add state.py test_state.py
git commit -m "feat: post state persistence"
```

---

### Task 5: Postagem no X (`post.py`)

**Files:**
- Create: `post.py`
- Test: `test_post.py`

**Interfaces:**
- Consumes: `state.load_next_index`, `state.save_next_index` (Task 4).
- Produces: `post_next(manifest, poster, next_index) -> int` (retorna o novo next_index — avança só se `poster` não levantar exceção); `main()` que lê `manifest.json`, monta o poster real (tweepy) e persiste o estado.

- [ ] **Step 1: Escrever o teste que falha (avanço de índice, poster fake)**

```python
# test_post.py
import pytest
from post import post_next

MANIFEST = [
    {"index": 0, "file": "frames/00001.jpg", "seconds": 0, "text": "oi"},
    {"index": 1, "file": "frames/00002.jpg", "seconds": 1, "text": ""},
]

def test_advances_on_success():
    posted = []
    def poster(file, text): posted.append((file, text))
    new_index = post_next(MANIFEST, poster, 0)
    assert new_index == 1
    assert posted == [("frames/00001.jpg", "oi")]

def test_does_not_advance_on_failure():
    def poster(file, text): raise RuntimeError("rate limit")
    with pytest.raises(RuntimeError):
        post_next(MANIFEST, poster, 0)

def test_end_of_manifest_returns_same_index():
    def poster(file, text): raise AssertionError("não deveria postar")
    assert post_next(MANIFEST, poster, 2) == 2
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `.venv/Scripts/python.exe -m pytest test_post.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'post'`.

- [ ] **Step 3: Implementar `post.py`**

```python
import json
import os
import sys
from pathlib import Path

from state import load_next_index, save_next_index

MANIFEST = "manifest.json"


def post_next(manifest: list[dict], poster, next_index: int) -> int:
    """Posta o item em next_index via poster(file, text). Avança só em sucesso.

    Se next_index passou do fim, não faz nada e retorna next_index.
    Se poster levanta exceção, ela propaga e o índice NÃO avança.
    """
    if next_index >= len(manifest):
        return next_index
    item = manifest[next_index]
    poster(item["file"], item["text"])
    return next_index + 1


def _tweepy_poster():
    """Constrói um poster(file, text) que posta imagem+texto no X via tweepy."""
    import tweepy

    auth = tweepy.OAuth1UserHandler(
        os.environ["X_API_KEY"], os.environ["X_API_SECRET"],
        os.environ["X_ACCESS_TOKEN"], os.environ["X_ACCESS_TOKEN_SECRET"],
    )
    api_v1 = tweepy.API(auth)  # upload de mídia é v1.1
    client_v2 = tweepy.Client(
        consumer_key=os.environ["X_API_KEY"],
        consumer_secret=os.environ["X_API_SECRET"],
        access_token=os.environ["X_ACCESS_TOKEN"],
        access_token_secret=os.environ["X_ACCESS_TOKEN_SECRET"],
    )

    def poster(file: str, text: str):
        media = api_v1.media_upload(filename=file)
        client_v2.create_tweet(text=text, media_ids=[media.media_id])

    return poster


def _load_env(path=".env"):
    """Carrega KEY=VALUE do .env para os.environ (sem dependência extra)."""
    p = Path(path)
    if not p.exists():
        return
    for line in p.read_text(encoding="utf-8").splitlines():
        line = line.strip()
        if not line or line.startswith("#") or "=" not in line:
            continue
        key, _, value = line.partition("=")
        os.environ.setdefault(key.strip(), value.strip())


def main():
    _load_env()
    manifest = json.loads(Path(MANIFEST).read_text(encoding="utf-8"))
    next_index = load_next_index()
    if next_index >= len(manifest):
        print("fim do vídeo — nada a postar")
        return
    new_index = post_next(manifest, _tweepy_poster(), next_index)
    save_next_index(new_index)
    print(f"postado frame {next_index} -> próximo {new_index}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: Rodar e ver passar**

Run: `.venv/Scripts/python.exe -m pytest test_post.py -v`
Expected: 3 passed.

- [ ] **Step 5: Rodar a suíte inteira**

Run: `.venv/Scripts/python.exe -m pytest -v`
Expected: todos passam (subtitles + prepare + state + post).

- [ ] **Step 6: Commit**

```bash
git add post.py test_post.py
git commit -m "feat: post next frame to X with state advance"
```

---

### Task 6: Credenciais do X + primeiro post real

Sem código novo — passo guiado. Documentar no README para referência futura.

**Files:**
- Modify: `README.md` (append seção "Credenciais do X")

- [ ] **Step 1: Guiar a criação da conta de dev do X**

Passos (fazer com o usuário):
1. Acessar https://developer.x.com e criar/entrar num projeto.
2. Em "User authentication settings", habilitar OAuth 1.0a com permissão **Read and Write**.
3. Gerar as 4 chaves: API Key, API Key Secret, Access Token, Access Token Secret.
4. Colar em `.env` (copiado de `.env.example`).

- [ ] **Step 2: Append no README a seção acima**

```markdown
## Credenciais do X

1. https://developer.x.com → criar projeto/app.
2. User authentication settings → OAuth 1.0a, permissão **Read and Write**.
3. Gerar API Key/Secret + Access Token/Secret → colar no `.env`.
```

- [ ] **Step 3: Post real de teste**

Run: `.venv/Scripts/python.exe post.py`
Expected: aparece 1 frame no perfil do X; `state.json` vira `{"next_index": 1}`.

- [ ] **Step 4: Commit**

```bash
git add README.md
git commit -m "docs: X credentials setup"
```

---

### Task 7: Agendador (a cada 1h30)

Escolher UMA opção conforme a hospedagem decidida. Documentar a escolhida.

**Files:**
- Create (se GitHub Actions): `.github/workflows/post.yml`
- OU Modify: `README.md` (instruções Task Scheduler / cron)

- [ ] **Step 1: Decidir a hospedagem** (GitHub Actions / Task Scheduler / VPS).

- [ ] **Step 2A (GitHub Actions): criar workflow**

```yaml
# .github/workflows/post.yml
name: post-frame
on:
  schedule:
    - cron: "0,30 */3 * * *"   # aprox. a cada 1h30 (00:00, 01:30, 03:00...)
  workflow_dispatch:
jobs:
  post:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.13" }
      - run: pip install -r requirements.txt
      - run: python post.py
        env:
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_TOKEN_SECRET: ${{ secrets.X_ACCESS_TOKEN_SECRET }}
      - run: |
          git config user.name "frame-bot"
          git config user.email "bot@users.noreply.github.com"
          git add state.json && git commit -m "state: advance" && git push || echo "sem mudança"
```

Nota: GitHub Actions cron não faz "a cada 90 min" direto; `0,30 */3` dá o ritmo médio de 1h30. `frames/` e `manifest.json` precisam estar versionados nesse cenário (ajustar `.gitignore`).

- [ ] **Step 2B (Task Scheduler Windows): documentar**

```markdown
## Agendador (Windows Task Scheduler)

1. Criar tarefa básica, gatilho: diário, repetir a cada 1h30.
2. Ação: iniciar programa
   - Programa: `C:\...\bot-every-frame-in-order\.venv\Scripts\python.exe`
   - Argumentos: `post.py`
   - Iniciar em: pasta do projeto.
3. Só posta com o PC ligado.
```

- [ ] **Step 3: Testar o gatilho manualmente** (workflow_dispatch / rodar a tarefa uma vez).

- [ ] **Step 4: Commit**

```bash
git add .github/workflows/post.yml README.md
git commit -m "ci: scheduled posting every ~90min"
```

---

## Notas de execução

- Ordem cronológica é garantida pela numeração zero-padded dos frames + `sorted()`, e pelo `next_index` monotônico.
- O único ponto que fala com a rede é `_tweepy_poster`; toda a lógica testável (casamento de legenda, avanço de estado) roda sem API.
- Trocar o intervalo pra 1h (plano pago) = só mudar o cron/tarefa; nenhum código muda.
